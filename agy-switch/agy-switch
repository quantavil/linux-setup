#!/usr/bin/env bash
set -euo pipefail
project_dir="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd)"
if [[ ! -x "$project_dir/target/release/agy-switch" ]]; then
  echo 'Build first: cargo build --release --locked' >&2
  exit 1
fi
exec "$project_dir/target/release/agy-switch" "$@"
